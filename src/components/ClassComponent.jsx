import React from 'react'
//Export default is use for only one element exporting, here we export only one element ClassComponent
//export const is used for some of chosed element exporting form a mddule
export default class ClassComponent extends React.Component{
    render(){
        return(
            <div>
                <h1>This is the example of class component</h1>
            </div>
        )
    }
}